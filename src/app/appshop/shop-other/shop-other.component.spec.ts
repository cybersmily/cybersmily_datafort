import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';

import { ShopOtherComponent } from './shop-other.component';

describe('ShopOtherComponent', () => {
  let component: ShopOtherComponent;
  let fixture: ComponentFixture<ShopOtherComponent>;
/*
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ShopOtherComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ShopOtherComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
  */
});

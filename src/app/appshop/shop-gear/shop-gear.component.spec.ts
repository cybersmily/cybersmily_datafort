import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';

import { ShopGearComponent } from './shop-gear.component';

describe('ShopGearComponent', () => {
  let component: ShopGearComponent;
  let fixture: ComponentFixture<ShopGearComponent>;
/*
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ShopGearComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ShopGearComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
*/
});


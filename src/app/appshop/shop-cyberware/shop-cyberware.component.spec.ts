import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ShopCyberwareComponent } from './shop-cyberware.component';

describe('ShopCyberwareComponent', () => {
  let component: ShopCyberwareComponent;
  let fixture: ComponentFixture<ShopCyberwareComponent>;
/*
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ShopCyberwareComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ShopCyberwareComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
  */
});

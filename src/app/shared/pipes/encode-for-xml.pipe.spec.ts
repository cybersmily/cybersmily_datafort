import { EncodeForXmlPipe } from './encode-for-xml.pipe';

describe('EncodeForXmlPipe', () => {
  it('create an instance', () => {
    const pipe = new EncodeForXmlPipe();
    expect(pipe).toBeTruthy();
  });
});

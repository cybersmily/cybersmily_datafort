import { OrderByPropsPipe } from './order-by-props.pipe';

describe('OrderByPropsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByPropsPipe();
    expect(pipe).toBeTruthy();
  });
});

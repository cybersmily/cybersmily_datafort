import { WeightNamePipe } from './weight-name.pipe';

describe('WeightNamePipe', () => {
  it('create an instance', () => {
    const pipe = new WeightNamePipe();
    expect(pipe).toBeTruthy();
  });
});

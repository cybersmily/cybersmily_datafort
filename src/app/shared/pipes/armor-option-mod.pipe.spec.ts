import { ArmorOptionModPipe } from './armor-option-mod.pipe';

describe('ArmorOptionModPipe', () => {
  it('create an instance', () => {
    const pipe = new ArmorOptionModPipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterWithArrayPipe } from './filter-with-array.pipe';

describe('FilterWithArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWithArrayPipe();
    expect(pipe).toBeTruthy();
  });
});

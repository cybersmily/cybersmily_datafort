import { MartialBonuses } from './martial-bonuses';

describe('MartialBonuses', () => {
  it('should create', () => {
    const bonuses = new MartialBonuses();
    expect(bonuses).toBeTruthy();
  });
});

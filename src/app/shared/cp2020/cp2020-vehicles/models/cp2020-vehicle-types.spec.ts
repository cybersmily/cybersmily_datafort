import { Cp2020VehicleTypes } from './cp2020-vehicle-types';

describe('Cp2020VehicleTypes', () => {
  it('should create an instance', () => {
    expect(new Cp2020VehicleTypes()).toBeTruthy();
  });
});

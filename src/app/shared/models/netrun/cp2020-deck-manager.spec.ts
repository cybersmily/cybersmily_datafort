import { Cp2020DeckManager } from './cp2020-deck-manager';

describe('Cp2020DeckManager', () => {
  it('should create an instance', () => {
    expect(new Cp2020DeckManager()).toBeTruthy();
  });
});

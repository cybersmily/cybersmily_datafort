/**
 * Interfrace that stores the mapping of an artile to an html page.
 * @export
 * @interface csArticle
 */
export interface CSArticle {
  name: string;
  title: string;
  html: string;
  updated: string;
  description: string;
}

import { CPRedFloorCostCalcPipe } from './c-p-red-floor-cost-calc.pipe';

describe('CPRedFloorCostCalcPipe', () => {
  it('create an instance', () => {
    const pipe = new CPRedFloorCostCalcPipe();
    expect(pipe).toBeTruthy();
  });
});

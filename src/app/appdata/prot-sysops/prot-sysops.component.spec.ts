import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';

import { ProtSysopsComponent } from './prot-sysops.component';

describe('ProtSysopsComponent', () => {
  let component: ProtSysopsComponent;
  let fixture: ComponentFixture<ProtSysopsComponent>;
/*
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ProtSysopsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ProtSysopsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
  */
});

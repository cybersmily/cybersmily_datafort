import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { Chrome2Component } from './chrome2.component';

describe('Chrome2Component', () => {
  let component: Chrome2Component;
  let fixture: ComponentFixture<Chrome2Component>;
/*
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ Chrome2Component ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(Chrome2Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
  */
});
